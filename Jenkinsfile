pipeline {
    agent any // instructs Jenkins to allocate an executor and workspace for the entire pipeline
    stages {
        stage("Hello") {
            steps {
                echo "Hello World"
                echo "World Hello"
            }
        }
    }
}